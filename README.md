# pyfgtconflib


This fork changes the parse_text to use a string as input instead of a file object. It creates a new method called parse_file which reads from a file. This will break the examples in the upstream repo
